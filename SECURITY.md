# Security policy

SafeChange executes inside OpenCode and may be granted permission to modify files
or run repository commands. Use a disposable branch or worktree, review the plan
before approving APPLYING state, and inspect the resulting diff before merging.

Do not place secrets in prompts, reports, example fixtures, or committed
configuration. Report security issues privately through GitHub's security
advisory feature once the repository is published.

