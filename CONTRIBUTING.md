# Contributing

Contributions should preserve the separation between read-only analysis and
approved mutation.

1. Create a focused branch.
2. Add tests for deterministic runtime changes.
3. Run `npm run check`.
4. Explain permission changes explicitly in the pull request.

Agent prompt changes should include an example demonstrating the behavior they
are intended to improve.

