# Example: public API change

```text
Analyze the impact of changing `parseConfig(path: string)` to
`parseConfig(options: ParseOptions)`. Identify every caller, adapter, test,
example, and exported type affected by the change. Propose a backwards-compatible
transition and a removal timeline. Do not edit files until I approve the plan.
```

